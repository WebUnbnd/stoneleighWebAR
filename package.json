{
  "name": "amplify-js-app",
  "version": "1.0.0",
  "description": "AWS Amplify JavaScript Example",
  "dependencies": {
    "aws-amplify": "^1.1.28",
    "netlify-cli": "^2.11.19"
  },
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "@mdx-js/loader": "^1.6.22",
    "babel-loader": "^8.3.0",
    "copy-webpack-plugin": "^11.0.0",
    "uglifyjs-webpack-plugin": "^2.1.3",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^3.1.5"
  },
  "scripts": {
    "start": "webpack && pushd ./serve && npm install && popd && ./serve/bin/serve -d dist -n",
    "start-windows": "webpack && pushd ./serve && npm install && popd && ./serve\\bin\\serve.bat -d dist -n",
    "build": "webpack"
  }
}
